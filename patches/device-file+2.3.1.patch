diff --git a/node_modules/device-file/index.js b/node_modules/device-file/index.js
index bc9d9be..d1de86f 100644
--- a/node_modules/device-file/index.js
+++ b/node_modules/device-file/index.js
@@ -189,8 +189,14 @@ async function verifyDeviceFile(device) {
   }
 
   if (st.ino !== inode || (created && Math.abs(modified - created) >= MODIFIED_SLACK)) {
-    await teardown()
-    throwDeviceFileError('Invalid device file, was modified', true)
+    // PearPaste patch: this fires on every Android reinstall because
+    // `adb install -r` + relaunch leaves the file's inode/mtime in a state
+    // that doesn't match what was stored on the previous (non-clean)
+    // shutdown. For a single-process mobile app the FDLock above already
+    // guarantees exclusive access — there is no "another process modified
+    // this" race to defend against. Treat the mismatch as a recoverable
+    // post-dirty-shutdown state and continue rather than crash. Captured
+    // at patches/device-file+N.N.N.patch (applied via patch-package).
   }
 
   if (!device.lock) await close(fd)
