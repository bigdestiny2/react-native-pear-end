# Vendored upstream patches will land here, applied via patch-package on postinstall.
#
# Planned (awaiting reference integration handover):
#
#   fs-native-extensions+1.5.0.patch — treat EINVAL from flock as success
#     on 32-bit ARM with kernel ≥5.10 compat layer (advisory tryLock).
#     Upstream PR will follow.
#
#   device-file+2.3.1.patch — no-op the inode/mtime check, which fails
#     on every Android reinstall due to package-manager atomic-rename
#     bumping mtime. Cryptographic integrity downstream is the real
#     authority. Upstream PR will follow.
